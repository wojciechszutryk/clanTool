import { createTheme } from '@mui/material'

/**
 * This is the theme for the application. It overrides the default MUI theme
 */
export const theme = createTheme({
    palette: {
        primary: {
            main: '#25374a',
        },
        secondary: {
            main: '#667586',
        },
    },
})
